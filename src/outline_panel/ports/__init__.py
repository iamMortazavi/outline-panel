"""The seams where a second implementation is real, and nowhere else.

One port, deliberately: `NodePort`. Shadowsocks is increasingly detected in the
markets this panel serves, and if Outline is widely blocked the rest of the
system is worth nothing without a second backend. That is a real second
implementation, not a hypothetical one.

There is no repository port, no unit-of-work, no notifier abstraction. Those
would each be an interface with exactly one implementation, which buys a
diagram and costs a file.
"""
