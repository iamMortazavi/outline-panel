# پلن اصلاحی Outline Panel

بازبینی کامل کد در تاریخ ۲۰۲۶-۰۷-۲۵ روی برنچ `claude/code-review-refactor-plan`.
پایه: ۱۵۱ تست سبز، ~۹۰۰۰ خط.

هر مورد با شناسه، شدت، فایل و خط، و «چرا بد است» آمده. ترتیب اجرا از بالا به پایین.

## وضعیت

| # | مورد | شدت | وضعیت |
|---|---|---|---|
| S1 | ربات: بالا بردن سطح دسترسی روی هر کلید | 🔴 حیاتی | ✅ انجام شد |
| S2 | ربات standalone: هر ادمین = مالک، و ساخت کلید خراب | 🔴 حیاتی | ✅ انجام شد |
| S3 | چهار مسیر رایگان برای ادمین اعتباری | 🔴 مالی | ✅ انجام شد |
| S4 | `can_see` روی لیست خالی باز بود | 🟠 بالا | ✅ انجام شد |
| S5 | ردیف یتیم ادمین | 🟡 متوسط | ✅ انجام شد |
| C1 | کش کهنه‌ی settings | 🟠 بالا | ✅ انجام شد |
| C2 | رجیستری کهنه بین پروسه‌ها | 🟠 بالا | ✅ انجام شد |
| C3/C4 | مقادیر هاردکد | 🟡 متوسط | ✅ انجام شد |
| P1 | ساب‌ادمین رمز خودش را عوض کند | 🟡 متوسط | ✅ انجام شد |
| P2 | هشدار به صاحب کلید برسد | 🟡 متوسط | ✅ انجام شد |
| P3 | فونت CDN | 🟡 متوسط | ✅ انجام شد |
| P4 | پنل کاربر نهایی | 🟢 توسعه | ✅ انجام شد |
| U1 | پلن شروع‌نشده «بدون انقضا» نمایش داده می‌شد | 🟠 بالا | ✅ انجام شد |

تست‌ها: ۱۵۱ → ۱۶۸ (۱۷ تست جدید در `tests/test_hardening.py`).
درستی تست‌های امنیتی با mutation بررسی شد: با کد قبلی fail می‌شوند.

---

## فاز ۱ — امنیت (حیاتی، همین حالا)

### S1 — ربات تلگرام: بالا بردن سطح دسترسی
`bot/dispatcher.py` — `cb_key`, `cb_link`, `cb_rename`, `cb_limit`, `cb_disable`,
`cb_enable`, `cb_del`, `cb_extend`

هر هشت هندلر فقط `gate(cq, cap)` را صدا می‌زنند که **تنها capability** را چک می‌کند.
`sid`/`kid` از `callback_data` می‌آید و callback_data را کلاینت می‌سازد.

نتیجه: ساب‌ادمینی که فقط به سرور A دسترسی دارد، با ساختن دستی
`disable:<sid_B>:<kid>` کاربر ادمین دیگر را روی سرور B غیرفعال می‌کند، و با
`link:<sid_B>:<kid>` لینک `ss://` هر کلید را برمی‌دارد.
داشبورد این را با `enforce_scope` / `assert_key_access` می‌بندد؛ ربات هیچ معادلی ندارد.

**اصلاح:** یک `gate_key(target, cap, sid, kid)` که علاوه بر cap، `can_see` و `owns`
را هم بررسی کند — یک تابع، تمام هندلرها از آن رد شوند.

### S2 — ربات standalone: هر ادمین رباتی = مالک پنل
`bot/run.py:60` دیسپچر را **بدون** `resolve_admin` و **بدون** `create_key` می‌سازد.

`dispatcher.py:94` مسیر fallback دارد:
```python
return {"is_owner": 1} if uid in await admin_ids() else None  # tests
```
یعنی در پروسه‌ی standalone هر آی‌دی داخل `bot_admin_ids` **مالک پنل** حساب می‌شود:
همه‌ی سرورها، همه‌ی کلیدها، بدون اسکوپ و بدون اعتبار.
ضمناً `create_key is None` است، پس ساخت کاربر از ربات standalone اصلاً کار نمی‌کند
(«Key creation is unavailable»).

**اصلاح:** `run.py` هم `settings.admin_for_telegram` و هم یک `create_key` واقعی تزریق کند.
مسیر fallback فقط برای تست بماند و در کد production غیرقابل‌دسترس شود.

### S3 — نشتی مالی: محصول رایگان برای ادمین اعتباری
`web/routers/keys.py`

| مسیر | مشکل |
|---|---|
| `PUT .../keys/{kid}/limit` | ادمین اعتباری حجم را روی هر عددی می‌گذارد، رایگان |
| `PUT .../keys/{kid}/monthly` | سهمیه‌ی ماهانه‌ی نامحدود، رایگان |
| `POST .../keys/{kid}/reset` | تمدید حجم هر دوره، رایگان |
| `POST /sub/{token}/servers/{target}` | یک کلید کامل روی سرور دوم، رایگان |

خودِ کد در `extend_key` نوشته: «هر زمان یا حجمی که به کاربر می‌رسد پول دارد».
این چهار مسیر دقیقاً همان قاعده را نقض می‌کنند. ساب‌ادمین یک پکیج ارزان می‌خرد و
بعد با این‌ها هرچه بخواهد اضافه می‌کند.

**اصلاح:** روی هر چهار مسیر، برای `on_credit(admin)` رد شود و به خرید پکیج ارجاع دهد.

### S4 — `can_see` روی لیست خالی باز است
`core/rights.py:38` — `return not allowed or sid in allowed`

لیست سرور خالی برای یک غیرمالک یعنی «همه‌ی سرورها». امروز API جلوی ساختن چنین
ادمینی را می‌گیرد، ولی restore بکاپ، ادیت مستقیم DB یا هر مسیر جدیدی این را
به دسترسی کامل بی‌صدا تبدیل می‌کند. پیش‌فرض باید fail-closed باشد.

### S5 — ردیف یتیم در ساخت ادمین
`web/routers/admins.py:111-112` — `db.add_admin` قبل از `_check_telegram` اجرا می‌شود.
اگر `telegram_id` تکراری باشد ۴۰۰ برمی‌گردد ولی ادمین **ساخته شده**.

---

## فاز ۲ — کش و مقدار هاردکد

### C1 — `SettingsStore._cache` کهنه می‌ماند
`core/settings.py:33` یک dict در حافظه‌ی همان پروسه است و هیچ‌وقت invalidate نمی‌شود.
وب‌اپ توکن ربات را عوض می‌کند → پروسه‌ی `outline-panel-bot` تا ری‌استارت خبر ندارد.
`backup.py:49` مجبور شده دستی `settings._cache.clear()` بزند — همان بوی مشکل.

**اصلاح:** کش حذف شود. خواندن یک ردیف از SQLite محلی ارزان‌تر از این دردسر است.

### C2 — `Registry` فقط یک‌بار در startup لود می‌شود
`web/registry.py:15` — سرورها در startup خوانده و در حافظه نگه داشته می‌شوند.
با چند worker یا با ربات standalone، سروری که در یک پروسه اضافه شد در بقیه دیده نمی‌شود.

**اصلاح:** رجیستری از DB بخواند و کلاینت‌های httpx را per (url, cert) نگه دارد.

### C3 — TTL متریک‌ها هاردکد
`core/outline_api.py:246` — `ttl: float = 15.0`. باید تنظیم قابل ویرایش باشد و `0` آن را خاموش کند.

### C4 — ثابت‌هایی که باید تنظیم باشند
| مقدار | جای فعلی |
|---|---|
| `NOTIFY_LIMIT_PERCENT`, `NOTIFY_EXPIRY_DAYS` | env فقط |
| `EXPIRY_CHECK_INTERVAL` | env فقط |
| `MONTH_SECONDS = 30*86400` | `core/utils.py:10` |
| `_UPDATE_INTERVAL_HOURS = 12` | `routers/subscription.py:31` |
| `_LOGIN_MAX_FAILS`, `_LOGIN_WINDOW`, `_GLOBAL_MAX_FAILS` | `routers/auth.py:28-30` |
| متریک TTL | `outline_api.py:246` |
| واحد پول («تومان») | متن UI |
| `SESSION_MAX_AGE` | env فقط |

**اصلاح:** جدول `settings` + یک `/api/settings/panel` (GET/PUT، owner-only).
env فقط bootstrap اولیه باشد، نه منبع حقیقت.

---

## فاز ۳ — سه پنل

### P1 — ساب‌ادمین نمی‌تواند رمز خودش را عوض کند
`/api/settings/password` مالک-فقط است و `/api/admins/{id}` هم.
یک فروشنده هیچ راهی برای چرخاندن رمز خودش ندارد.

### P2 — هشدارها به آدم اشتباه می‌رسد
`bot/manager.py:111 notify()` به کل `bot_admin_ids` پیام می‌دهد.
کاربرِ ساب‌ادمین که منقضی می‌شود، به مالک خبر می‌رسد و به فروشنده‌ی صاحب کلید نه.
باید بر اساس `owner_admin_id` → `admins.telegram_id` مسیریابی شود.

### P3 — فونت گوگل از CDN
`static/index.html:8-10` سه ریکوئست به `fonts.googleapis.com` / `fonts.gstatic.com`.
این پنل برای مناطق فیلترشده است؛ آنجا این دامنه‌ها بلاک‌اند → تأخیر و درز اطلاعات.
فونت‌های سیستمی جایگزین شوند.

### U1 — پلن شروع‌نشده «بدون انقضا» نمایش داده می‌شد 🟠
`routers/subscription.py` — تا وقتی کاربر وصل نشده `expiry_ts` برابر NULL است،
پس `_collect` مقدار `expire: 0` برمی‌گرداند و صفحه می‌نوشت **«No expiry»**.
یعنی به کسی که همین الان ۳۰ روز خریده گفته می‌شد اشتراکش هیچ‌وقت تمام نمی‌شود.

**اصلاح:** فیلد `pendingDays` اضافه شد. صفحه حالا می‌نویسد
«Validity: 30 days — starts on first connection» با رنگ هشدار.
دو تست: حالت pending، و حالت واقعاً بی‌انقضا که نباید pending شود.

### P4 — پنل کاربر نهایی ✅
`sub.html` از قبل بهتر از چیزی بود که در پیش‌نویس این پلن نوشته بودم:
QR برای هر سرور، نوار مصرف و دکمه‌ی کپی **از قبل وجود داشت**. آن بند اشتباه بود.

شکاف واقعی جای دیگری بود: کاربر یک لینک می‌گرفت و **هیچ راهنمایی نداشت** با آن
چه کند. بخش **Connect** اضافه شد:

- تشخیص خودکار دستگاه (iPhone / Android / Windows / macOS / Linux) + امکان
  تعویض دستی، چون خیلی‌ها لینک را روی دسکتاپ باز می‌کنند و راهنمای موبایل می‌خواهند.
- سه گام ساده: نصب کن → Add بزن → وصل شو.
- برای هر دستگاه، اپ‌های مناسب با دکمه‌ی **Add** که با دیپ‌لینک اشتراک را
  مستقیم وارد اپ می‌کند (Hiddify، Streisand، Shadowrocket، v2rayNG، Clash، sing-box).
- دکمه‌ی **Open** روی هر سرور: `ss://` اسکیم خود Outline است، پس کانفیگ را
  مستقیم به اپ رسمی می‌دهد.
- اگر اپ نصب نباشد اسکیم بی‌صدا نادیده گرفته می‌شود، پس یک toast می‌گوید چه کند
  و دکمه‌ی کپی همیشه به‌عنوان fallback سر جایش است.

**یک جدول `APPS` عمداً هاردکد است.** نام اپ‌ها و اسکیم‌هایشان تنها چیزی در این
صفحه است که هر یکی‌دو سال کهنه می‌شود؛ یک جای مشخص برای ویرایش بهتر از پخش‌شدن
آن‌ها در کد است. تبدیلش به تنظیمات پنل (۶ اپ × ۵ پلتفرم) ارزشش را ندارد.

**آنچه نساختم:** ورود واقعی کاربر نهایی (ریست لینک، تاریخچه‌ی خرید). آن نیاز به
هویت کاربر دارد و توکن اشتراک برایش کافی نیست — تصمیم معماری بزرگ‌تری است که
باید خودتان بگیرید.

---

## آنچه عمداً دست‌نخورده می‌ماند

- **کش تک‌پروازه‌ی متریک‌ها** حذف نمی‌شود، قابل تنظیم می‌شود. حذف کامل یعنی هر
  ۵ ثانیه یک ریکوئست به endpoint آزمایشی هر سرور Outline — همان کاری که کش برای
  جلوگیری از آن ساخته شده. `metrics_ttl=0` خاموشش می‌کند.
- **`_wants_html` heuristic** در `subscription.py` — خودش با `ponytail:` علامت خورده
  و `?format=raw` راه فرار دارد.
- **rate-limit در حافظه** — با یک worker درست کار می‌کند. سقفش قابل تنظیم می‌شود؛
  انتقالش به DB وقتی ارزش دارد که واقعاً چند worker اجرا شود.
