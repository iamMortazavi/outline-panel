# نقشه‌راه نسخه‌ی بعدی

نوشته شده بعد از بازبینی کامل کد (۲۰۲۶-۰۷-۲۵). پیش‌نیاز: `REFACTOR_PLAN.md`.

این فهرست آرزو نیست. هر بند سه چیز دارد: **چرا** (چه دردی را درمان می‌کند)،
**چه** (تغییر مشخص در کد)، و **هزینه**. بندهایی هم هست که عمداً **نباید** ساخته شوند —
آن بخش به اندازه‌ی بقیه مهم است.

اندازه‌ها: `S` ≈ ۱–۲ روز · `M` ≈ ۱ هفته · `L` ≈ ۲–۴ هفته · `XL` ≈ ۱–۳ ماه

---

## وضعیت اجرا

| بند | وضعیت |
|---|---|
| 0.1 مهاجرت واقعی | ✅ |
| 0.3 لاگ ممیزی | ✅ |
| 1.1 تکرارناپذیری پول | ✅ |
| 0.2 چند-worker | ✅ |
| 1.2 مغایرت‌گیری | ✅ |
| 3.4 محافظت مسیر عمومی | ✅ |
| 3.1 بکاپ خودکار | ✅ |
| 3.2 مشاهده‌پذیری | ✅ |
| 0.4 چندزبانه | ⬜ |
| 1.3 درگاه پرداخت | ⬜ |
| 1.4 فاکتور | ⬜ |
| 2.x مشتری نهایی | ⬜ (منتظر تصمیم 4.1) |
| 3.3 سلامت سرورها | ⬜ |
| 4.1 پروتکل‌های دیگر | ⬜ تصمیم شماست |

---

## اگر فقط سه کار بکنید

۱. **`0.1` مهاجرت واقعی دیتابیس** — هر فاز بعدی جدول اضافه می‌کند. اگر این را
   آخر بگذارید، باید سیستم مهاجرت را مهاجرت بدهید.
۲. **`0.3` لاگ ممیزی** — چند فروشنده به پول دست می‌زنند و هیچ ردی نمی‌ماند.
   روزی که سر یک تراکنش اختلاف بشود، دیر است.
۳. **`1.1` کلید تکرارناپذیری روی پول** — همین الان یک ریکوئست retry شده دو بار
   پول کم می‌کند. ارزان‌ترین اصلاح با بیشترین اثر در کل این سند.

هر سه با هم حدود دو هفته. بقیه می‌تواند صبر کند؛ این سه تا نه.

---

## فاز ۰ — پایه‌ها (قبل از هر چیز دیگر)

### 0.1 مهاجرت واقعی دیتابیس · `S`
**چرا.** `db.init()` الان **۱۱** دستور `PRAGMA table_info` / `ALTER TABLE` پشت هم
دارد که با هر ستون جدید بلندتر می‌شود. `PRAGMA user_version` **اصلاً استفاده نشده**.
این الگو نمی‌تواند ستون حذف کند، داده مهاجرت بدهد، یا بگوید دیتابیس در چه نسخه‌ای است.

**چه.** یک لیست مرتب از توابع مهاجرت + `PRAGMA user_version` به‌عنوان شمارنده:

```python
_MIGRATIONS = [_m001_initial, _m002_owner_admin_id, _m003_credit, ...]

async def _migrate(self):
    cur = await self.conn.execute("PRAGMA user_version")
    have = (await cur.fetchone())[0]
    for i, fn in enumerate(_MIGRATIONS[have:], start=have + 1):
        await fn(self.conn)
        await self.conn.execute(f"PRAGMA user_version = {i}")
    await self.conn.commit()
```

**Alembic نه.** برای SQLite تک‌فایلی با این اندازه، وابستگی و مفهوم اضافه می‌آورد
بدون اینکه چیزی حل کند که این ۱۰ خط حل نمی‌کند.

**ریسک.** مهاجرت‌های موجود باید یک‌بار به این شکل بازنویسی شوند و روی یک دیتابیس
واقعیِ قدیمی تست شوند، نه فقط روی دیتابیس خالی تست‌ها.

### 0.2 درستی چند-worker · `M`
**چرا.** الان پنل فقط با یک worker درست کار می‌کند و این جایی نوشته نشده.

سه چیز process-local است:
- `auth._login_fails` — با N worker یعنی N برابر بودجه‌ی حمله.
- **زمان‌بند**: هماهنگی‌اش یک فلگ دستی است (`ENABLE_SCHEDULER`). اگر اپراتور
  اشتباه ست کند، دو پروسه هم‌زمان کلیدها را reset و منقضی می‌کنند.
- رجیستری — **این یکی حل شد** (`reg.sync()`).

**چه.** یک جدول قفلِ اجاره‌ای، و زمان‌بند فقط وقتی می‌چرخد که اجاره را گرفته باشد:

```sql
CREATE TABLE locks (name TEXT PRIMARY KEY, holder TEXT, expires_ts INTEGER);
-- UPDATE locks SET holder=?, expires_ts=? WHERE name='scheduler' AND expires_ts < ?
```

آن‌وقت `ENABLE_SCHEDULER` حذف می‌شود: هر تعداد worker بالا بیاورید، دقیقاً یکی
زمان‌بندی می‌کند و اگر بمیرد، اجاره منقضی و دیگری برمی‌دارد.
Rate limit هم به همان جدول یا یک جدول کوچک می‌رود.

**نکته‌ی SQLite.** با WAL و `busy_timeout=5000` چند worker مشکلی ندارد، ولی
`synchronous=NORMAL` را ست کنید و بدانید همه‌ی نوشتن‌ها سریالایز می‌شوند.
تا چند هزار کلید کاملاً کافی است.

### 0.3 لاگ ممیزی · `M`
**چرا.** چند ساب‌ادمین به کلید و اعتبار دست می‌زنند و **هیچ ردی نمی‌ماند**.
«چه کسی این کاربر را حذف کرد؟» «چه کسی قیمت را عوض کرد؟» جوابی ندارد.
`credit_ledger` فقط پول را می‌بیند، نه بقیه‌ی کارها.

**چه.** جدول `audit_log(actor_admin_id, action, target_type, target_id, before, after, ip, ts)`
و یک dependency روی روترهای تغییردهنده — نه فراخوانی دستی در هر تابع، وگرنه
همان‌جایی که مهم است یادتان می‌رود.

نگهداری: چرخش بر اساس سن، قابل تنظیم در همان `KNOBS`.

### 0.4 چندزبانه + RTL · `L`
**چرا.** بازار فارسی‌زبان است و کل رابط انگلیسی. مهم‌تر: **۴۲ رشته‌ی خام انگلیسی**
از بک‌اند به‌صورت `detail="..."` مستقیم در UI نمایش داده می‌شود.

**چه.** بخش سختش بک‌اند است، نه فرانت:
1. هر `HTTPException` یک **کد** بگیرد در کنار متن: `detail={"code": "credit.insufficient", "params": {...}}`.
2. فرانت از روی کد ترجمه کند و متن انگلیسی fallback بماند.
3. `dir="rtl"` + تنظیم زبان per-admin.

**هشدار.** این تغییر به قرارداد API دست می‌زند. اگر کسی به شکل `detail` وابسته
شده، می‌شکند. الان که فقط UI خودتان مصرف‌کننده است، ارزان‌ترین زمان انجامش است.

---

## فاز ۱ — چرخه‌ی پول

### 1.1 تکرارناپذیری روی عملیات پولی · `S` ⚠️ **باگ فعلی**
**چرا.** `create_key` و `extend_key` اول پول کم می‌کنند بعد به Outline زنگ می‌زنند.
اگر پاسخ به کلاینت نرسد و کاربر دوباره بزند، **دو بار پول کم می‌شود و دو کلید ساخته
می‌شود**. `charge()` اتمیک است ولی اتمیک بودن جلوی تکرار را نمی‌گیرد.

**چه.** هدر `Idempotency-Key` روی مسیرهای خرید + جدولی که کلید و پاسخ را نگه دارد؛
تکرار همان کلید، همان پاسخ را برمی‌گرداند بدون اجرای دوباره.

ارزان‌ترین بند این سند با بیشترین اثر. **همین اول انجام شود.**

### 1.2 مغایرت‌گیری اعتبار · `S`
**چرا.** `ledger_sum(admin)` باید همیشه با `admins.credit` برابر باشد. برای این
یک **تست** هست ولی در زمان اجرا **هیچ‌چیز چک نمی‌کند**. اگر روزی drift کند، بی‌صدا drift می‌کند.

**چه.** یک پاس در زمان‌بند که این تساوی را چک کند و اختلاف را به مالک هشدار بدهد.

### 1.3 درگاه پرداخت · `L` 💰 **بزرگ‌ترین گشایش تجاری**
**چرا.** الان مالک باید دستی موجودی هر فروشنده را شارژ کند. یعنی مالک گلوگاه
هر فروش است و شب‌ها هم باید جواب بدهد.

**چه.**
- جدول `payments(admin_id, provider, amount, ref, status, created_ts, verified_ts)`.
- یک interface با **یک** پیاده‌سازی برای شروع (زرین‌پال یا IDPay)، نه سه تا.
- کریپتو (USDT TRC20) در این بازار خیلی رایج است — گام دوم.
- **قاعده‌ی طلایی:** اعتبار فقط و فقط از مسیر `credit_admin()` و فقط بعد از
  تأیید callback با امضای معتبر حرکت کند. هرگز از روی پارامترهای redirect.

**ریسک.** callback درگاه‌ها ناپایدار است؛ حتماً «تأیید فعال» (query وضعیت تراکنش)
هم داشته باشید نه فقط انتظار برای webhook.

### 1.4 فاکتور و رسید · `S`
یک صفحه‌ی قابل چاپ برای هر تراکنش. برای فروشنده‌ای که به مشتری رسید می‌دهد لازم است.

---

## فاز ۲ — مشتری نهایی

### 2.1 هویت مشتری · `L` 🏗 **بزرگ‌ترین تغییر مدل داده**
**چرا.** امروز «کاربر» = یک access key + یک توکن اشتراک. مشتری واقعی نمی‌تواند
حساب داشته باشد، چند اشتراک داشته باشد، تاریخچه ببیند یا خودش تمدید کند.
هر قابلیت مشتری‌محور به این بند گره خورده است.

**چه.** جدول `customers` + `keys.customer_id`. توکن اشتراک می‌ماند (لینک‌های موجود
نباید بشکنند) ولی دیگر تنها هویت نیست.

**قبلش تصمیم بگیرید:** ورود مشتری با چه چیزی؟ شماره موبایل + OTP؟ تلگرام؟
ایمیل؟ در این بازار **تلگرام** کم‌اصطکاک‌ترین است و زیرساختش را از قبل دارید.

### 2.2 ربات فروشگاهی مشتری · `L`
**چرا.** در این بازار بیشتر فروش داخل تلگرام اتفاق می‌افتد، نه روی وب.

**چه.** رباتی جدا از ربات ادمین: دیدن پلن‌ها، پرداخت، دریافت کانفیگ، دیدن مصرف،
تمدید. روی `1.3` و `2.1` سوار می‌شود.

**هشدار.** این را با ربات ادمین **یکی نکنید**. دو مخاطب کاملاً متفاوت با دو
مدل دسترسی؛ ادغامشان همان اشتباهی است که در `bot/run.py` دیدیم.

### 2.3 تمدید از صفحه‌ی اشتراک · `M`
وقتی `1.3` و `2.1` باشند، یک دکمه‌ی «تمدید» در `sub.html`. صفحه از قبل آماده است.

---

## فاز ۳ — عملیات

### 3.1 بکاپ خودکار · `S/M`
**چرا.** بکاپ الان دستی است و باید مالک یادش بیفتد. `VACUUM` هیچ‌جای کد نیست.

**چه.** `VACUUM INTO '/path/backup-<ts>.db'` — پریمیتیو درست SQLite: اتمیک، بدون
قفل کردن، بدون نیاز به توقف. + چرخش بر اساس تعداد/سن، آپلود اختیاری به تلگرام یا S3،
و **یک تست restore** که واقعاً بازیابی کند.

### 3.2 مشاهده‌پذیری · `M`
لاگ ساخت‌یافته با request id، و یک `/metrics` (کلید ساخته‌شده، شارژ، تأخیر و خطای
تماس با Outline). الان اگر Outline کند شود هیچ‌جا دیده نمی‌شود.

### 3.3 سلامت سرورهای بالادست · `M`
دسترس‌پذیری فقط لحظه‌ای و per-request محاسبه می‌شود. تاریخچه نگه دارید و روی
قطعی ممتد هشدار بدهید.

### 3.4 محدودسازی مسیر عمومی اشتراک · `S` ⚠️
**چرا.** `/sub/{token}` احراز هویت ندارد و **هر بار** به همه‌ی سرورهای Outline
ریکوئست می‌زند. کسی که این را بکوبد، بار را روی تمام سرورها پخش می‌کند.

**چه.** کش کوتاه per-token + محدودیت نرخ per-IP. (توکن ۲۲ کاراکتری امن است؛
مشکل brute-force نیست، مشکل تقویت بار است.)

---

## فاز ۴ — شرط بزرگ: پروتکل‌های دیگر

### 4.1 انتزاع backend · `XL` 🎲 **این یک تصمیم است، نه یک تسک**

**چرا.** Shadowsocks/Outline در ایران روزبه‌روز بیشتر تشخیص داده می‌شود.
VLESS+Reality چیزی است که الان دوام می‌آورد. اگر Outline گسترده بلاک شود،
**بقیه‌ی این سند بی‌معنی است.**

**چه.** `OutlineAPI` پشت یک پروتکل بیاید:
`list_keys / create_key / delete_key / set_limit / usage`. بعد Xray یا sing-box
به‌عنوان backend دوم (VLESS+Reality، Trojan).

**صادقانه:** این هویت محصول را عوض می‌کند — از «پنل Outline» به «پنل VPN».
اسم ریپو، مستندات، و بخش زیادی از `keys.py` و `sub.html` را لمس می‌کند.

**توصیه‌ی من:** انتزاع را **الان** انجام ندهید (YAGNI با یک پیاده‌سازی)، ولی
**تصمیمش را عقب نیندازید**. اگر جواب «بله می‌خواهیم»، این باید قبل از فاز ۲
بیاید، چون `2.1` مدل داده را دور کلید Outline قفل می‌کند.

---

## آنچه نباید ساخته شود

| چیز | چرا نه |
|---|---|
| بازنویسی با React/Vue | سه صفحه است و vanilla جواب می‌دهد. build step بیشتر از چیزی که برمی‌گرداند هزینه دارد. به‌جایش `index.html` را (۱۱۶۷ خط) به چند فایل بشکنید. |
| مهاجرت به Postgres | SQLite با WAL خیلی بیشتر از این بار را تحمل می‌کند. وقتی **واقعاً** contention دیدید مهاجرت کنید، نه از روی حدس. |
| میکروسرویس / Kubernetes | یک پنل تک‌مستأجره است. یک پروسه و یک فایل دیتابیس، ویژگی است نه بدهی. |
| GraphQL | مصرف‌کننده‌ی API خودتان هستید. REST فعلی دقیقاً اندازه است. |
| Event sourcing برای اعتبار | `credit_ledger` از قبل append-only است — بخش خوب ماجرا را دارید. بقیه‌اش مراسم است. |
| Redis برای کش | چیزی که کش می‌خواست را همین هفته حذف کردیم چون کهنه می‌ماند. سرویس دوم اضافه نکنید. |

---

## ترتیب پیشنهادی

```
0.1 مهاجرت ──┬─ 0.3 ممیزی ── 0.2 چند-worker ── 0.4 i18n
             └─ 1.1 idempotency ── 1.2 مغایرت ── 1.3 درگاه ── 1.4 فاکتور
                                                      │
                          [تصمیم 4.1: پروتکل؟] ───────┤
                                                      ↓
                                   2.1 هویت مشتری ── 2.2 ربات فروش ── 2.3 تمدید

فاز ۳ (عملیات) موازی است — 3.4 را همین حالا بزنید، ارزان است.
```

**نقطه‌ی تصمیم:** قبل از `2.1` باید تکلیف `4.1` روشن باشد. بعد از `2.1` تغییر
دادنش چند برابر گران‌تر می‌شود.
